version: "3.8"

services:
  spark:
    image: apache/spark:3.5.1
    container_name: spark
    command: >
      bash -c "
      /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master --host spark &
      sleep 5 &&
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark:7077
      "
    ports:
      - "7077:7077"
      - "8080:8080"
    networks:
      - spark-net

  livy:
    image: openeuler/livy:0.8.0-oe2403sp1
    container_name: livy
    depends_on:
      - spark
    ports:
      - "8998:8998"
    volumes:
      - ./livy/livy.conf:/opt/livy/conf/livy.conf
    networks:
      - spark-net

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: minio
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - ./minio/data:/data
    networks:
      - spark-net

networks:
  spark-net:
